class Game {
    constructor(thisIsWhitePlayer){
        this.thisIsWhitePlayer = thisIsWhitePlayer
        this.cessBoard = this.makeStartingBoard()
    }

    // make the board orientation accoring to player
    makeStartingBoard(){
        
        if (this.thisIsWhitePlayer){
            // for white player

        } else {
            // for black player

        }
    }

    // move the chess piece
    movePiece(pieceId, to){
        
    }

}